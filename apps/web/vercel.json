{
	"framework": "vite",
	"installCommand": "cd ../.. && bun install",
	"buildCommand": "cd ../.. && turbo build --filter=@kyra/web && bun build apps/api/src/vercel.ts --outfile apps/web/api/index.mjs --target node --format esm --packages bundle",
	"outputDirectory": "dist",
	"rewrites": [
		{ "source": "/api/(.*)", "destination": "/api" },
		{ "source": "/((?!api/).*)", "destination": "/index.html" }
	]
}
