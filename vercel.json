{
	"framework": null,
	"installCommand": "bun install",
	"buildCommand": "bunx vite build --root apps/web --outDir ../../dist",
	"outputDirectory": "dist",
	"rewrites": [
		{ "source": "/api/(.*)", "destination": "/api" },
		{ "source": "/((?!api/).*)", "destination": "/index.html" }
	]
}
